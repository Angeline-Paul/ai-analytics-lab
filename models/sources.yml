version: 2

sources:
  - name: raw_data
    description: Source data loaded through dbt seeds

    tables:
      - name: orders
        description: Raw customer order transactions

      - name: customers
        description: Raw customer master data